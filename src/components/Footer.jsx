import React, {Component} from 'react';
import PropTypes from 'prop-types';

const Footer = () => {
    return(
        <footer id={"Footer"}>
            Footer
        </footer>
    );
}

Footer.propTypes = {

};

export default Footer;